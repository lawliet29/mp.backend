import * as React from 'react'
import * as ReactDOM from 'react-dom'
import RootComponent from './components/components.root'

ReactDOM.render(
    <RootComponent />,
    document.getElementById('content')
);